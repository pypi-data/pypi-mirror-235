[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "palebluedot"
version = "0.0.1"
dependencies = [
    "scipy",
    "numpy",
    "Pillow",
]

[project.scripts]
bluedot = "palebluedot.cli:run"

[tool.hatch.build]
packages = ["src/palebluedot"]
