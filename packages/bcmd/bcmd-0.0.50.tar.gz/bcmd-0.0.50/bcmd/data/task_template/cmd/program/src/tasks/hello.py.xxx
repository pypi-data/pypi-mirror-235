from typing import Final

from beni import btask
from beni.bfunc import syncCall
from common import config

app: Final = btask.app


@app.command()
@syncCall
async def hello():
    '打印 hello'
    print(f'hello {config.msg}')
