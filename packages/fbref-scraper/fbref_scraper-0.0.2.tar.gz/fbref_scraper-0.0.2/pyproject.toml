[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "fbref_scraper"
version = "0.0.2"
authors = [
  { name="Lukas Rücker", email="ruecker.lukas@gmail.com" },
]
description = "Python package to scrape game and player stats from fbref website"
readme = "README.md"
dependencies = ['pandas','bs4','requests']
requires-python = ">=3.7"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]

[project.urls]
"Homepage" = "https://github.com/lurue101/fbref_scraper"