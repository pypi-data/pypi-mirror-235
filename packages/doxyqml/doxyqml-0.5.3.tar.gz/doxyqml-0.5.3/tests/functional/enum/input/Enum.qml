Item {
    /// Enum type
    /// in multiple lines
    enum Direction {
        Up, ///< Go up
        /// Go down
        Down
    }

    enum IsThisOk { ///< Inline comment
        /** Comma after last entry */
        CommaAfterLast,
    }
}
