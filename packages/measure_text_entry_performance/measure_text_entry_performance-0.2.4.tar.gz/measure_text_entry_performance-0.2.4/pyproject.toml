[tool.poetry]
name = "measure_text_entry_performance"
version = "0.2.4" # use poetry-dynamic-versioning
description = "library to measure text entry performance"
authors = ["2ndr9"]
repository = "https://github.com/2ndr9/measure_text_entry_performance"
readme = "README.md"

[tool.poetry-dynamic-versioning]
enable = false
style = "pep440"

[tool.poetry.dependencies]
python = "^3.9"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
