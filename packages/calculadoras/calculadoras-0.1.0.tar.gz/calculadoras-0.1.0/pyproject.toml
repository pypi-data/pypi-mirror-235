[tool.poetry]
name = "calculadoras"
version = "0.1.0"
description = "Calculadora par aoperaciones basicas"
authors = ["Andre"]
license = "MIT"
readme = "README.md"
packages = [{ include = "calculadora", from = "src" }]

[tool.poetry.dependencies]
python = ">=3.11.1,<3.13"
pandas = "^2.1.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
