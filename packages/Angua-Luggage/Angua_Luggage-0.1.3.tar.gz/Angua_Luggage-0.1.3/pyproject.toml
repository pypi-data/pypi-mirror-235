[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "Angua_Luggage"
version = "0.1.3"

[tool.pytest.ini_options]
testpaths = ["test"]