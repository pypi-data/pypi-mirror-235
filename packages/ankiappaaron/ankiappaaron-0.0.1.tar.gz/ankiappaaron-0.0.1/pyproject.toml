[project]
name = "ankiappaaron"
description = "Utilities for AnkiApp"
version = "0.0.1"
authors = [
    { name = "Aaron Alphabet", email = "aaron.alphabet@posteo.org" }
]
dependencies = [
]
readme = "README.md"
requires-python = ">=3.10"
license = {file = "LICENSE.txt"}

[build-system]
requires = ["setuptools>=43.0.0", "wheel"]
build-backend = "setuptools.build_meta"