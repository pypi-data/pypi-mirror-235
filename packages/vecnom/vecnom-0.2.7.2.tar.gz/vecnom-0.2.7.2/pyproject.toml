[tool.poetry]
name = "vecnom"
version = "0.2.7.2"
description = "Python SDK for Vectify AI."
authors = ["Ray <rejojer@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.8"
requests = "^2.25.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

