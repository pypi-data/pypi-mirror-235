[build-system]
requires = ["setuptools>=61.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "deepfellow"
version = "0.2.1"
description = "DeepFellow"
authors = [{ name = "Simplito" }]
dependencies = [ "torchserve" ]

