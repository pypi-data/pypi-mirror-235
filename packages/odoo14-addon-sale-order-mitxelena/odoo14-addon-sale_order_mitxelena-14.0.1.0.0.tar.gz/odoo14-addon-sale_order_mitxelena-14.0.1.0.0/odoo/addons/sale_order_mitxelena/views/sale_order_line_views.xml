<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- Tree view for sale.order.line -->
    <record id="view_order_line_tree" model="ir.ui.view">
        <field name="name">sale.order.line.tree</field>
        <field name="model">sale.order.line</field>
        <field name="arch" type="xml">
            <tree string="Sale Order Lines">
                <field name="order_id"/>
                <field name="sequence"/>
                <field name="product_id"/>
                <field name="product_uom_qty"/>
                <field name="price_unit"/>
                <field name="price_subtotal"/>
                <field name="dimensions"/>
                <field name="materials"/>
            </tree>
        </field>
    </record>

    <!-- Form view for sale order -->
    <record id="view_order_form_custom" model="ir.ui.view">
        <field name="name">sale.order.form.custom</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='order_line']/tree/field[@name='product_uom_qty']" position="after">
                <field name="dimensions"/>
                <field name="materials"/>
            </xpath>
            <xpath expr="//field[@name='payment_term_id']" position="after">
                <field name="mecanization"/>
                <field name="welding"/>
                <field name="painting"/>
                <field name="assembly"/>
            </xpath>
        </field>
    </record>
    
    <!-- Action to open tree view -->
    <record id="action_order_lines" model="ir.actions.act_window">
        <field name="name">Sale Order Lines</field>
        <field name="res_model">sale.order.line</field>
        <field name="view_mode">tree</field>
        <field name="view_id" ref="view_order_line_tree"/>
    </record>

    <!-- Menu item to access tree view -->
    <menuitem id="menu_order_lines" name="Sale Order Lines" parent="sale.sale_menu_root" sequence="20" action="action_order_lines" groups="sales_team.group_sale_salesman_all_leads"/>
</odoo>
