[build-system]
requires = [
    "setuptools >= 65",
    "wheel >= 0.38",
]
build-backend = "setuptools.build_meta"

[project]
name = "kv_text_to_speech"
authors = [{name = "Justin Bui", email = "jmbui@klangverso.com"}]
version = "0.1.0"
requires-python = ">=3.8"
dependencies = [
    "azure-cognitiveservices-speech",
]